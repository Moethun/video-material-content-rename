---
name: video-material-content-rename
description: 批量把视频/图片素材按画面内容识别后重命名，并转写语音生成可检索的素材索引。当用户给出一个装满无意义编号名的素材文件夹（如 C0769.MP4、DJI_0001.MP4、MVI_1234.MP4）并要求"按内容重命名/总结素材内容/整理这批素材/给素材改名/找出哪些是口播素材/给素材建索引"时使用。包含抽帧、联系表拼图、视觉识别、whisper 语音转写、安全重命名与一键回滚的完整流程。
---

[English](SKILL.md) | 中文

# 素材批量按内容重命名 + 语音转写

把 `C0769.MP4` 变成 `博主出镜_01.MP4`，同时给每条素材补一层音频信息：**有没有人声、说了什么**。

画面告诉你「拍的是什么」，声音告诉你「讲的是什么」。两个信号合起来才是完整的素材画像。

## 定位

这个 skill 不是"全自动 AI 命名器"，而是 **AI-in-the-loop 的素材入库工作流**：

```
probe 侦察去重  →  frames 抽帧拼图  →  [AI 看联系表识别]  →  plan 生成方案  →  apply 执行
                                              ↑
                                    transcribe 语音转写（并行补充信号）
```

机械部分交给脚本，语义识别交给 AI 看联系表。核心设计是**用拼图把几百次读图压缩成十几次**——
286 个素材抽帧后拼成 18 张联系表，AI 读 18 次就能完成识别，成本降一个数量级。

## 铁律：先安全，再动手

1. **第一遍只读侦察**（`probe.py`）：文件数 / 总大小 / 格式分布 / 有无子目录 / 有没有已经命好名的文件。先出报告，别动文件。
2. **绝不用文件大小判重**。同一设备同参数连拍的切片会出现大量字节数完全相同的文件（实测 286 个里有 40 个都是 29365858 字节），但内容各不相同。必须全量 MD5。
3. **重命名是破坏性操作**，执行前必须给「原名→新名」完整对照表 + 加粗提示风险 + 拿到用户明确确认。
4. **重命名不改文件内容，所以不需要复制整份素材作备份**。「映射表 + 回滚脚本」就是完整备份，明确告诉用户这一点。
5. **两阶段改名防碰撞**（`apply.py` 已内置）：先把所有原文件挪到临时名，再落到最终名。否则 A→B、B→C 这类交换会互相覆盖。
6. **分批准入**：每 10 个文件复核一次目录文件数，任何一步异常立刻停下并回滚。
7. 用户已有的好名字默认**不动**，只处理编号名——但要主动问一句。

## 环境准备

```bash
pip install -r requirements.txt
```

- **不需要系统级 ffmpeg**：`imageio-ffmpeg` 自带二进制，用 `imageio_ffmpeg.get_ffmpeg_exe()` 取路径。
  注意它**不含 `ffprobe`**，探时长/音轨一律用 `ffmpeg -i <file>`（不带输出参数）读 stderr。
- **语音用 `faster-whisper`，不要用 `openai-whisper`**：后者依赖 torch（装完 2GB+、常驻内存几百 MB），前者只需 ctranslate2 + onnxruntime（约 75MB），纯 wheel 无需编译，CPU 快 4-5 倍。
- 首次运行会从 HuggingFace 下载模型。国内网络建议设 `HF_ENDPOINT=https://hf-mirror.com`（脚本已默认设置）。

## 流程

### 1. 侦察与查重 — `scripts/probe.py`

```bash
python scripts/probe.py --src "D:/素材"
```
产出 `_report.txt`（人看的报告）和 `_hashmap.tsv`。零重复是常态，但必须验证过才能说。

### 2. 抽帧 + 拼联系表 — `scripts/frames.py`

```bash
python scripts/frames.py --src "D:/素材" --cols 4 --rows 4
```
- 抽帧位置默认取 **50%**，避开片头黑场和对焦过程。
- 拼图每格下方带「序号 + 原文件名」标签，缩略图等比缩放后居中裁切，不变形。
- 中文标签栏会自动找系统里可用的中文字体（微软雅黑 / 苹方 / Noto CJK / 文泉驿）。
- 266 帧 → 18 张联系表（4×4）。**让 AI 逐张读联系表，不要逐张读单帧图。**

### 3. 语音转写 — `scripts/transcribe.py`

```bash
python scripts/transcribe.py --src "D:/素材" --out transcript.tsv --model small
```
给素材补听觉信号，产出 `name | duration | voice(Y/N) | nsegs | avg_logprob | text`。

**模型档位（中文实测）**

| 模型 | 结论 |
|---|---|
| tiny | **不可用** —— 短音频陷入重复幻觉（同一句刷屏） |
| base | 勉强 —— 仍有重复，短片段基本无信息 |
| **small** | **推荐** —— 10 秒以上片段可直接出逐字稿 |
| medium / large | 仅在口播是核心资产、且不在乎耗时时用 |

**时长 vs 有语音（286 个素材实测，可直接当预筛规则）**

| 素材时长 | 有语音比例 |
|---|---|
| <5s | 约 35% |
| 5-10s | 约 67% |
| **≥10s** | **100%** |

用 `--only-longer-than 10` 只转长素材，命中率 100%，算力砍掉一半以上。

**参数为什么这么设（全部实测踩过）**

- `initial_prompt=None` —— **绝对不要传领域引导词**。传了之后模型在无语音片段会把 prompt 原文当结果显示（实测凭空复读 4 次）。想靠引导词改善专有名词，代价是污染，不划算；用术语纠错表在事后修更干净。
- `condition_on_previous_text=False` —— 不关掉，短音频必然陷入重复循环。
- `vad_filter=True` + `min_silence_duration_ms=500` —— VAD 门禁是本方案最有价值的部分，能可靠区分口播和空镜。
- `no_speech_threshold=0.6` / `log_prob_threshold=-1.0` / `compression_ratio_threshold=2.4` / `temperature=[0,0.2,0.4]` —— 一组抑制幻觉的联合阈值，缺一个都会漏出垃圾文本。
- **简繁必须统一**：Whisper 中文简繁输出不稳定，实测同一条里混出「按一下按鈕就完事機器為什麼能打印」。用 `zhconv` 转一道再纠错。
- 术语纠错表见 `examples/terms.example.tsv`，按自己行业替换。

**音频走管道，不落临时文件**：`ffmpeg ... -f s16le -ar 16000 -ac 1 -` 直接输出到 stdout，`np.frombuffer` 转 float32 喂给 `model.transcribe(arr)`。有些运行环境有"安全删除"类钩子，批量处理时反复删临时文件会被拦截甚至中断脚本（实测踩过）。

### 4. 生成方案 + 干跑校验 — `scripts/plan.py`

```bash
python scripts/plan.py --src "D:/素材" --labels labels.txt --out-dir "D:/out" --transcript transcript.tsv
```
**这一步不改文件**，只产出方案。

标签文件两种格式，自动识别：
- **位置模式**：每行一个标签，顺序 = 文件名排序顺序
- **映射模式**：`文件名<TAB>标签`（不怕错位，推荐）

新名规则：`标签_序号.扩展名`，序号在**同一标签内**按原文件名顺序递增——
这样同类素材的先后与拍摄顺序一致，剪辑时能按序取。

干跑会检查：源文件存在 / 目标名唯一 / 无非法字符 / 不与目录里未参与重命名的文件冲突 / 标签长度。
把 `label` 数控制在 **20-40 类**，每类 3-50 个，太长会全是单例，太短失去区分度。

### 5. 执行与回滚 — `scripts/apply.py`

```bash
python scripts/apply.py --src "D:/素材" --mapping _mapping.tsv            # 预览
python scripts/apply.py --src "D:/素材" --mapping _mapping.tsv --apply    # 执行
python scripts/apply.py --src "D:/素材" --reverse                          # 一键还原
```
回滚表 `_rename_done.tsv` 写进素材目录，边执行边落盘，中途断电也能还原。回滚后自动归档为 `_rename_done.reversed.tsv`，不阻塞下一次执行。

## 语音结果怎么用

**用**：
- **口播 / 空镜分流** —— `voice=Y` 是有人声的，`N` 是纯画面 B-roll。这是最有价值的输出，剪辑时直接按这个筛。
- **口播文案清单** —— 筛 `text` 长度 ≥20 的，按长度降序导成纯文本，就是一份可直接复用的文案素材库。
- **重复机位识别** —— 同一段讲解往往在多机位各拍一遍。文本做 2-gram Jaccard ≥0.45 聚类，同组选一条即可。
- **素材索引** —— 文本入库后可按关键词找素材。

**不要**：
- **不要用转写文本直接命名**。短片段转出来是"放 下一个 拿出来"这种碎片，拿来命名会毁掉名字质量。**画面仍是命名的主依据**，语音只作辅助和索引。
- 不要指望 10 秒以下的片段出完整句子。
- 不要在转写结果上做严格的事实核对。它是"听感还原"，同音错字和漏字是常态，用来检索和参考可以，用来引用原文不行。

## 其他坑

- **Python 旧式 `%` 格式化拼 HTML 会炸**：CSS 里的 `100%`、`aspect-ratio` 会让 `%` 后跟 `;` 报 `ValueError: unsupported format character ';'`。用 `.replace("__PLACEHOLDER__", ...)` 占位符。
- **Windows PowerShell 5.1 重定向 Python stdout 会把中文写成乱码**（显示层乱码，文件本身没错），且输出文件可能被当成二进制。让 Python 自己用 `encoding="utf-8"` 写结果文件最可靠。
- **给 Excel 用的 tsv/csv 加 BOM**（`utf-8-sig`），否则 Excel 打开乱码。
- **后台任务报 `completed` 但日志中途截断**，先怀疑脚本被钩子或异常中断，去查真实产出文件的行数，别信任务状态。
- **抽帧脚本用增量模式**（帧比源文件新就跳过），避免每轮清理删文件触发批量删除限制。

## 交付清单

- `重命名对照表.html` — 缩略图 + 原名→新名（+ 语音文本），给用户核对
- `重命名对照表.tsv` — 表格版
- `素材语音索引.tsv` — 文件名 / 时长 / 有无口播 / 转写文本
- `口播文案清单.txt` — 纯文本文案素材库
- 汇报必带三项复核数字：**文件数未变 / 总大小未变 / 无残留编号名**

## 和下游工具的衔接

按脚本对槽位做粗剪的工具，可以直接消费本 skill 产出的语音索引，不必重新抽帧识别一遍。
粗剪前先看 `voice=Y` 的素材，口播自然对得上。
